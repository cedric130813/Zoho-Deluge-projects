info user;
userFirstName = user.get("first_name");
crm_manual = "<INSERT LINK>";
response = Map();
response.put("text","Hi " + userFirstName + ", thanks for your request. This is the manual: " + crm_manual + " :smile:");
return response;
